# Railway Environment Variables Template for Medusa v2
# Copy these to your Railway project's environment variables

# Memory allocation for Node.js (critical for admin build)
NODE_OPTIONS=--max-old-space-size=4096

# Database configuration (Railway will provide this)
DATABASE_URL=${{Postgres.DATABASE_PUBLIC_URL}}

# Redis configuration (Railway will provide this)
REDIS_URL=${{Redis.REDIS_PUBLIC_URL}}

# Medusa configuration
MEDUSA_WORKER_MODE=server
DISABLE_MEDUSA_ADMIN=false
MEDUSA_BACKEND_URL=https://YOUR_RAILWAY_URL.railway.app

# Security secrets (generate strong values)
JWT_SECRET=supersecret_jwt_token_change_this
COOKIE_SECRET=supersecret_cookie_secret_change_this

# CORS settings
STORE_CORS=*
ADMIN_CORS=*
AUTH_CORS=*

# Runtime settings
PORT=9000
NODE_ENV=production